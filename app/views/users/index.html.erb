<%= render 'books/list', user: @user, book: @book %>

 <h2>Users</h2>
  <% @users.each do |user| %>
  <div>
   <p><%= image_tag user.get_profile_image(100,100) %></p>
   <p>name：<%= user.name %></p>
  </div>
  <% end %>
  
 


